{
  "name": "carepoint-backend",
  "version": "1.0.0",
  "description": "CarePoint API and Static Build Server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd client && npm install --include=dev && npm run build && cd ../server && npm install"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "dependencies": {
  "react": "^18.x.x",
  "react-dom": "^18.x.x",
  "vite": "^5.x.x",
  "@vitejs/plugin-react": "^4.x.x"
}
}